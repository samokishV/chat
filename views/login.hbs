<div class="cover">
    <div class="inner-cover">
        <form method="post" action="" class="registration-form needs-validation" novalidate>
            <h5 class="text-uppercase bold main-header"> Log in </h5>
            {{#if errors}}
                <div class="alert alert-danger" role="alert">
                    {{#each errors}}
                        <li>{{this.msg}}</li>
                    {{/each}}
                </div>
            {{/if}}
            <div class="form-group">
                <label for="name">Login</label>
                <input type="text" id="name" name="login" class="form-control" value="{{login}}" required>
            </div>
            <div class="form-group">
                <label for="password">Password</label>
                <input type="password" id="password" name="password" class="form-control" value="{{password}}" required>
            </div>
            <input type="submit" class="btn btn-success btn-lg w-100" value="Log in">
        </form>
    </div>
</div>